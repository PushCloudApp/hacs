[pytest]
asyncio_mode = auto
testpaths = tests
# pytest-homeassistant-custom-component turns Home Assistant's own warnings into
# errors for core integrations. A custom component is not held to that, and the
# noise buries real failures.
filterwarnings =
    ignore::DeprecationWarning
